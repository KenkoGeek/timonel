image:
  repository: nginx
  tag: '1.27'
replicas: 3
database:
  enabled: true
  host: localhost
  port: 5432
  credentials:
    username: admin
    password: secret123
features:
  monitoring:
    enabled: false
    endpoint: http://prometheus:9090
  cache:
    enabled: true
    config:
      maxSize: 100MB
      ttl: 3600
      compression: true
secrets:
  apiKey: my-secret-api-key
  certificate: |-
    -----BEGIN CERTIFICATE-----
    MIIC...
    -----END CERTIFICATE-----
